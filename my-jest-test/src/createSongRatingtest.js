import React from "react" ;
import {render,screen,fireEvent, getByTestId} from "@testing-library/react" ;
import CreateSongRating from "./createSongRating";
import "@testing-library/jest-dom" ;
/*
describe("Rendering Tests", () => {

    
    it("renders the create rating form", () => {
        render(<CreateSongRating />);
        const inputElement = screen.getByRole("form");
        expect(inputElement).toBeInTheDocument();
    });
    /*
    it("renders the info", () => {
        render(<CreateSongRating />);
        const inputElement = screen.getByRole("info");
        expect(inputElement).toBeInTheDocument();
    });

    it("renders the song rating list", () => {
        render(<CreateSongRating />);
        const inputElement = screen.getByRole("songList");
        expect(inputElement).toBeInTheDocument();
    });*/

}) ;*/